const config = require("config");

module.exports = function () {
  // throw new Error("Something failed during startup");
  // if (!config.get("jwtPrivatekey")) {
  //   throw new Error("FATALE ERROR : jwtPrivateKey is not defined");
  // }
};
